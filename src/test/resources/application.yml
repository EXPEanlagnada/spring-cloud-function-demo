application:
  title: "Spring Cloud Function Demo"
  version: 'HEAD'

spring:
  application:
    name: ${application.title}
  profiles:
    active: test

logging:
  level:
    root: info
    org.springframework: warn
    ch.qos.logback: warn
    com.kapresoft.demo: info
